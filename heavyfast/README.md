# HeavyIQ - Natural Language Interface for HeavyDB

HeavyIQ is a python module that serves as a natural language interface to interact with HeavyDB.

## Setup

Install requirements and then run the project.

```bash
$ python3.10 -m venv venv
$ . venv/bin/activate
$ pip install -r requirements.txt
$ python start.py
```